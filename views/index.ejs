<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alquiler de Bicicletas Eléctricas y Scooters Urbanos</title>
 
<title>Alquiler de Bicicletas Eléctricas y Scooters Urbanos</title>
<meta name="description" content="">


<title>Alquiler de Bicicletas Eléctricas y Scooters Urbanos</title>
<meta name="description" content="">
<meta property="og:url" content="https://p2-30765082-mv5f.onrender.com">
<meta property="og:type" content="website">
<meta property="og:title" content="Alquiler de Bicicletas Eléctricas y Scooters Urbanos">
<meta property="og:description" content="">
<meta property="og:image" content="https://opengraph.b-cdn.net/production/images/908e7948-2ce5-45da-aa45-00736db4e07f.jpg?token=QF39tJTF5pAuFxD_L8zyP5nP8ZjFgrvWW3_RvFiJccM&height=1800&width=1200&expires=33286024435">


<meta name="twitter:card" content="summary_large_image">
<meta property="twitter:domain" content="p2-30765082-mv5f.onrender.com">
<meta property="twitter:url" content="https://p2-30765082-mv5f.onrender.com">
<meta name="twitter:title" content="Alquiler de Bicicletas Eléctricas y Scooters Urbanos">
<meta name="twitter:description" content="">
<meta name="twitter:image" content="https://opengraph.b-cdn.net/production/images/908e7948-2ce5-45da-aa45-00736db4e07f.jpg?token=QF39tJTF5pAuFxD_L8zyP5nP8ZjFgrvWW3_RvFiJccM&height=1800&width=1200&expires=33286024435">

</head>
<body>
    <header>
        <h1><%= __('site_title') %></h1>
        <form method="get" action="/" style="margin:10px 18px 0 0;float:right;">
            <label for="lang" style="color:#2fd0f5;font-weight:bold;"><%= __('language') %>:</label>
            <select name="lang" id="lang" onchange="this.form.submit()" style="margin-left:8px;">
                <option value="es" <%= locale === 'es' ? 'selected' : '' %>><%= __('spanish') %></option>
                <option value="en" <%= locale === 'en' ? 'selected' : '' %>><%= __('english') %></option>
            </select>
        </form>
        <nav>
            <ul style="display:flex;flex-wrap:wrap;gap:10px;justify-content:center;align-items:center;">
                <li><a href="#descripcion" style="display:flex;align-items:center;"><span style="font-size:1.6em;margin-right:6px;">📝</span><i class="fas fa-info-circle"></i> <%= __('description') %></a></li>
                <li><a href="#beneficios" style="display:flex;align-items:center;"><span style="font-size:1.6em;margin-right:6px;">🌱</span><i class="fas fa-star"></i> <%= __('benefits') %></a></li>
                <li><a href="#galeria" style="display:flex;align-items:center;"><span style="font-size:1.6em;margin-right:6px;">📸</span><i class="fas fa-images"></i> <%= __('gallery') %></a></li>
                <li><a href="#contact-form" style="display:flex;align-items:center;"><span style="font-size:1.6em;margin-right:6px;">✉️</span><i class="fas fa-envelope"></i> <%= __('contact') %></a></li>
                <li><a href="#payment-form" style="display:flex;align-items:center;"><span style="font-size:1.6em;margin-right:6px;">💳</span><i class="fas fa-credit-card"></i> <%= __('payment') %></a></li>
                <li><a href="#contacto" style="display:flex;align-items:center;"><span style="font-size:1.6em;margin-right:6px;">🌐</span><i class="fas fa-share-alt"></i> <%= __('social_networks') %></a></li>
                <li><a href="/login" style="display:flex;align-items:center;"><span style="font-size:1.6em;margin-right:6px;">🔑</span><%= __('login') %></a></li>
            </ul>
        </nav>
    </header>
    <% if (typeof adminError !== 'undefined' || (typeof query !== 'undefined' && query.adminError)) { %>
      <div id="admin-error-modal" style="position:fixed;top:0;left:0;width:100vw;height:100vh;background:rgba(0,0,0,0.35);z-index:9999;display:flex;align-items:center;justify-content:center;">
        <div style="background:#fff;padding:32px 24px;border-radius:12px;box-shadow:0 2px 16px #0002;max-width:90vw;text-align:center;">
          <h3 style="color:#b02a37;margin-bottom:12px;">Solo los administradores pueden iniciar sesión</h3>
          <button onclick="document.getElementById('admin-error-modal').style.display='none'" style="margin-top:18px;padding:8px 24px;border-radius:8px;background:#007BFF;color:#fff;border:none;cursor:pointer;">Cerrar</button>
        </div>
      </div>
      <script>
        setTimeout(function(){
          var modal = document.getElementById('admin-error-modal');
          if(modal) modal.style.display = 'none';
        }, 60000); // 1 minuto
      </script>
    <% } %>
    <section id="descripcion">
        <h2><%= __('service_description_title') %></h2>
        <p><%= __('service_description_text') %></p>
    </section>

    <section id="beneficios">
        <h2><%= __('benefits_title') %></h2>
        <ul>
            <li><strong><%= __('benefit_fast') %></strong></li>
            <li><strong><%= __('benefit_carbon') %></strong></li>
            <li><strong><%= __('benefit_access') %></strong></li>
            <li><strong><%= __('benefit_price') %></strong></li>
        </ul>
    </section>
    <section id="galeria">
        <h2><%= __('gallery_title') %></h2>
        <div class="gallery-slider" style="overflow:hidden;max-width:100%;">
            <div class="gallery-track">
                <div class="gallery-item">
                    <div class="gallery-caption"><%= __('gallery_bike_electric') %></div>
                    <img src="/images/bike1.jpg" alt="<%= __('gallery_bike_electric') %>" style="width:300px; border-radius:8px; margin:0 10px;">
                </div>
                <div class="gallery-item">
                    <div class="gallery-caption"><%= __('gallery_scooter_urban') %></div>
                    <img src="/images/scooter1.jpg" alt="<%= __('gallery_scooter_urban') %>" style="width:300px; border-radius:8px; margin:0 10px;">
                </div>
                <div class="gallery-item">
                    <div class="gallery-caption"><%= __('gallery_bike_city') %></div>
                    <img src="/images/bici.jpg" alt="<%= __('gallery_bike_city') %>" style="width:300px; border-radius:8px; margin:0 10px;">
                </div>
            </div>
        </div>
    </section>

    <section id="contact-form">
        <h2><%= __('contact_form_title') %></h2>
        <form action="/contact/add" method="POST">
            <label for="email"><%= __('email') %>:</label>
            <input type="email" id="email" name="email" required>

            <label for="name"><%= __('names') %>:</label>
            <input type="text" id="name" name="name" required>

            <label for="comment"><%= __('comment') %>:</label>
            <textarea id="comment" name="comment"></textarea>

            <div class="g-recaptcha" data-sitekey="<%= RECAPTCHA_SITE_KEY %>"></div>
            <button type="submit" class="glow-on-hover"><%= __('send') %></button>
        </form>

     
    <div id="contact-confirmation-message" style="display: none; text-align: center; margin-top: 20px;">
        <p style="font-size: 18px; color: #007BFF;"><%= __('contact_form_success') %></p>
    </div>

    <section id="payment-form">
        <h2><%= __('payment_form_title') %></h2>
        <form id="paymentForm" action="/payment/add" method="POST">
            <label for="email"><%= __('email') %>:</label>
            <input type="email" id="paymentEmail" name="email" required>

            <label for="cardholderName"><%= __('cardholder_name') %>:</label>
            <input type="text" id="cardholderName" name="cardholderName" required>

            <label for="cardNumber"><%= __('card_number') %>:</label>
            <input type="text" id="cardNumber" name="cardNumber" pattern="^\d{13,19}$" title="<%= __('card_number_title') %>" required>

            <label for="expiryMonth"><%= __('expiry_month') %>:</label>
            <input type="number" id="expiryMonth" name="expiryMonth" min="1" max="12" required>

            <label for="expiryYear"><%= __('expiry_year') %>:</label>
            <input type="number" id="expiryYear" name="expiryYear" min="2025" required>

            <label for="cvv"><%= __('cvv') %>:</label>
            <input type="text" id="cvv" name="cvv" pattern="^\d{3,4}$" title="<%= __('cvv_title') %>" required>

            <label for="amount"><%= __('amount_to_pay') %>:</label>
            <input type="number" id="amount" name="amount" min="1000" step="10" title="<%= __('amount_title') %>" required>

            <label for="currency"><%= __('currency') %>:</label>
            <select id="currency" name="currency" required>
                <option value="USD">USD</option>
                <option value="EUR">EUR</option>
                <option value="MXN">MXN</option>
            </select>

            <label for="servicio"><%= __('service') %>:</label>
            <select id="servicio" name="servicio" required>
                <option value="Bicicleta eléctrica"><%= __('electric_bike') %></option>
                <option value="Scooter urbano"><%= __('urban_scooter') %></option>
                <option value="Accesorio"><%= __('accessory') %></option>
                <option value="Otro"><%= __('other') %></option>
            </select>

            <button type="submit" class="glow-on-hover"><%= __('pay') %></button>
        </form>
        <div id="payment-message" style="display:none; margin: 25px auto 0 auto; max-width: 420px; padding: 18px 24px; border-radius: 12px; font-size: 18px; font-weight: 500; text-align: center; box-shadow: 0 2px 12px rgba(0,0,0,0.07);"></div>
    </section>

   

    <section id="contacto">
        <h2><%= __('contact_us') %></h2>
        <p><%= __('follow_us') %></p>
        <a href="https://www.facebook.com/ehenzo.bretpo" target="_blank"><i class="fab fa-facebook"></i> Facebook</a>
        <a href="https://twitter.com" target="_blank"><i class="fab fa-twitter"></i> Twitter</a>
        <a href="https://www.instagram.com/sad_h.b?igsh=YzljYTk1ODg3Zg==" target="_blank"><i class="fab fa-instagram"></i> Instagram</a>
    </section>


    <footer>
        <p>&copy; 2025 <%= __('site_title') %>. <%= __('all_rights_reserved') %></p>
    </footer>
<style>
body{
    background:#1b1b1b;
    font-family: 'Poppins', Arial, sans-serif;
    line-height: 1.6;
    margin: 0;
    padding: 0;
}
header {
    background: #007BFF;
    color: #ffffff;
    padding: 10px 0 0 0;
    text-align: center;
}
header h1 {
    font-size: 2.1rem;
    color: #2fd0f5;
    margin: 18px 0 10px 0;
    letter-spacing: 1px;
}
nav ul {
    list-style: none;
    padding: 0;
    margin-bottom: 0;
}
nav ul li {
    display: inline;
    margin: 0 15px;
}
nav ul li a {
    color: #ffffff;
    text-decoration: none;
    font-weight: 500;
    font-size: 1.08rem;
    transition: color 0.2s;
}
nav ul li a:hover {
    color: #2fd0f5;
    text-decoration: underline;
}
section {
    padding: 28px 24px;
    margin: 32px auto;
    background: #252525;
    border-radius: 14px;
    box-shadow: 0 8px 32px 0 rgba(31,38,135,0.13);
    max-width: 600px;
    color: #fff;
}
h2 {
    color: #2fd0f5;
    border-bottom: 2px solid #2fd0f5;
    padding-bottom: 5px;
    text-align: center;
    margin-bottom: 18px;
}
footer {
    text-align: center;
    padding: 10px 0;
    background: #007BFF;
    color: #ffffff;
    position: relative;
    bottom: 0;
    width: 100%;
}
form input, form textarea, form select {
    width: 100%;
    padding: 10px 5px;
    margin: 16px 0 8px 0;
    border: none;
    border-bottom: 2px solid #2fd0f5;
    background: transparent;
    color: #0099ff;
    font-size: 16px;
    outline: none;
    border-radius: 30px;
    transition: border 0.3s;
}
form input:focus, form textarea:focus, form select:focus {
    border-bottom: 2px solid #57AAB4;
    box-shadow: 0 0 10px #2fd0f5;
    transform: scale(1.02);
}
form button {
    background: linear-gradient(to right, #03a9f4,#57AAB4,#03a9f4);
    color: #fff;
    padding: 12px 0;
    border: none;
    border-radius: 30px;
    font-weight: bold;
    font-size: 17px;
    cursor: pointer;
    transition: background 0.3s, transform 0.2s;
    box-shadow: 0 0 10px #2fd0f5;
    margin-top: 18px;
    width: 100%;
}
form button:hover {
    background:#57AAB4;
    color: #fff;
    box-shadow: 0 0 20px #57AAB4;
    transform: scale(1.05);
}
form label {
    display: block;
    margin-bottom: 5px;
    font-weight: bold;
    color: #2fd0f5;
}
form textarea {
    resize: none;
}
.gallery-slider {
    position: relative;
    width: 100%;
    height: 220px;
    margin: 0 auto 18px auto;
    background: transparent;
}
.gallery-track {
    display: flex;
    align-items: center;
    height: 100%;
    animation: gallery-move 12s linear infinite;
}
.gallery-track img {
    flex-shrink: 0;
    height: 200px;
    object-fit: cover;
    box-shadow: 0 2px 12px #0005;
    background: #fff;
}
.gallery-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin: 0 10px;
}
.gallery-caption {
    color: #2fd0f5;
    font-weight: bold;
    font-size: 1.08rem;
    margin-bottom: 6px;
    text-align: center;
    letter-spacing: 0.5px;
}
@keyframes gallery-move {
    0% { transform: translateX(0); }
    90% { transform: translateX(-100%); }
    100% { transform: translateX(0); }
}
@media (max-width: 700px) {
    section {
        max-width: 98vw;
        padding: 18px 4vw;
    }
    header h1 {
        font-size: 1.2rem;
    }
}
</style>

<script>
let contactIdCounter = 1; 

const contactForm = document.querySelector('#contact-form form');
contactForm.addEventListener('submit', async function (event) {
    event.preventDefault(); 
    const confirmationMessage = document.getElementById('contact-confirmation-message');
    confirmationMessage.style.display = 'none';

    const recaptchaResponse = grecaptcha.getResponse();
    if (!recaptchaResponse) {
        confirmationMessage.innerHTML = `<div style="background:#fff0f0;border:2px solid #dc3545;color:#b02a37;border-radius:10px;padding:18px 10px;line-height:1.5;font-size:18px;text-align:center;max-width:420px;margin:0 auto;">❌ <b><%= __('recaptcha_confirm') %></b></div>`;
        confirmationMessage.style.display = 'block';
        return;
    }

    try {
        const formData = new URLSearchParams();
        formData.append('email', contactForm.email.value);
        formData.append('name', contactForm.name.value);
        formData.append('comment', contactForm.comment.value);
        formData.append('g-recaptcha-response', recaptchaResponse);
        for (let [key, value] of formData.entries()) {
            console.log('FORMDATA', key, value);
        }
        const response = await fetch('/contact/add', {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
            body: formData.toString()
        });
        const result = await response.json();
        if (result.success) {
            confirmationMessage.innerHTML = `<div style="background:#eafaf1;border:2px solid #28a745;color:#218838;border-radius:10px;padding:18px 10px;line-height:1.5;font-size:18px;text-align:center;max-width:420px;margin:0 auto;">✔️ <b><%= __('contact_form_success') %></b></div>`;
            confirmationMessage.style.display = 'block';
            contactForm.reset();
            grecaptcha.reset(); 
        } else {
            confirmationMessage.innerHTML = `<div style="background:#fff0f0;border:2px solid #dc3545;color:#b02a37;border-radius:10px;padding:18px 10px;line-height:1.5;font-size:18px;text-align:center;max-width:420px;margin:0 auto;">❌ <b>${result.message || `<%= __('contact_form_error') %>`}</b></div>`;
            confirmationMessage.style.display = 'block';
        }
    } catch (error) {
        confirmationMessage.innerHTML = `<div style="background:#fff0f0;border:2px solid #dc3545;color:#b02a37;border-radius:10px;padding:18px 10px;line-height:1.5;font-size:18px;text-align:center;max-width:420px;margin:0 auto;">❌ <b><%= __('contact_form_error') %></b></div>`;
        confirmationMessage.style.display = 'block';
    }
});


const paymentForm = document.getElementById('paymentForm');
const paymentMessage = document.getElementById('payment-message');
paymentForm.addEventListener('submit', async function(event) {
    event.preventDefault();
    paymentMessage.style.display = 'none';
    paymentMessage.innerHTML = '';
    const formData = new URLSearchParams();
    formData.append('email', paymentForm.email.value);
    formData.append('cardholderName', paymentForm.cardholderName.value);
    formData.append('cardNumber', paymentForm.cardNumber.value);
    formData.append('expiryMonth', paymentForm.expiryMonth.value);
    formData.append('expiryYear', paymentForm.expiryYear.value);
    formData.append('cvv', paymentForm.cvv.value);
    formData.append('amount', paymentForm.amount.value);
    formData.append('currency', paymentForm.currency.value);
    formData.append('servicio', paymentForm.servicio.value);
    try {
        const response = await fetch('/payment/add', {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
            body: formData.toString()
        });
        const result = await response.json();
        let bg = '#eafaf1';
        let border = '#28a745';
        let color = '#218838';
        let icon = '✔️';
        let msg = '';
        if (result.success) {
            msg = `${icon} <b><%= __('payment_success') %></b>`;
            if (result.data && result.data.transaction_id) {
                msg += `<br><span style='font-size:15px;color:#555;'><%= __('transaction_id') %>: <b>${result.data.transaction_id}</b></span>`;
            }
        } else {
            bg = '#fff0f0';
            border = '#dc3545';
            color = '#b02a37';
            icon = '❌';
            msg = `${icon} <b>${result.message || 'Pago rechazado o error desconocido.'}</b>`;
        }
        paymentMessage.innerHTML = `<div style="background:${bg};border:2px solid ${border};color:${color};border-radius:10px;padding:18px 10px;line-height:1.5;">${msg}</div>`;
        paymentMessage.style.display = 'block';
        if (result.success) paymentForm.reset();
    } catch (error) {
        paymentMessage.innerHTML = `<div style="background:#fff0f0;border:2px solid #dc3545;color:#b02a37;border-radius:10px;padding:18px 10px;line-height:1.5;">❌ <b>Error al procesar el pago. Intente nuevamente.</b></div>`;
        paymentMessage.style.display = 'block';
    }
});

document.querySelector('#payment-form form').addEventListener('submit', function (event) {
    event.preventDefault(); 
    const confirmationMessage = document.getElementById('payment-confirmation-message');
    confirmationMessage.style.display = 'block'; 
    setTimeout(() => {
        this.submit(); 
    }, 2000); 
});

function showContactPreview(event) {
    event.preventDefault(); 

   
    const id = contactIdCounter++; 
    const email = document.getElementById('email').value;
    const name = document.getElementById('name').value;
    const comment = document.getElementById('comment').value;
    const ip = '127.0.0.1'; 
    const timestamp = new Date().toLocaleString(); 

 
    document.getElementById('preview-id').textContent = id;
    document.getElementById('preview-email').textContent = email;
    document.getElementById('preview-name').textContent = name;
    document.getElementById('preview-comment').textContent = comment;
    document.getElementById('preview-ip').textContent = ip;
    document.getElementById('preview-timestamp').textContent = timestamp;

    document.getElementById('email').value = email;
    document.getElementById('name').value = name;
    document.getElementById('comment').value = comment;

    document.getElementById('contact-preview').style.display = 'block';
}

window.addEventListener('DOMContentLoaded', function () {
    // Localización de fechas en la tabla de contactos
    const dateCells = document.querySelectorAll('#contact-list tbody tr td:nth-child(6)');
    dateCells.forEach(cell => {
        const raw = cell.textContent;
        if (!raw || raw === 'Fecha y Hora') return;
        const date = new Date(raw);
        if (!isNaN(date.getTime())) {
            let locale = '<%= locale %>';
            let options = { year: 'numeric', month: '2-digit', day: '2-digit', hour: '2-digit', minute: '2-digit' };
            if (locale === 'en') {
                options = { ...options, hour12: true };
                cell.textContent = date.toLocaleString('en-US', options);
            } else {
                options = { ...options, hour12: false };
                cell.textContent = date.toLocaleString('es-VE', options);
            }
        }
    });

    // Localización de montos en formularios y mensajes
    const amountInputs = document.querySelectorAll('input[name="amount"]');
    amountInputs.forEach(input => {
        input.addEventListener('blur', function() {
            let value = parseFloat(input.value);
            if (!isNaN(value)) {
                let locale = '<%= locale %>';
                let currency = document.getElementById('currency')?.value || (locale === 'en' ? 'USD' : 'VES');
                let formatted = value.toLocaleString(locale === 'en' ? 'en-US' : 'es-VE', {
                    style: 'currency',
                    currency: currency,
                    minimumFractionDigits: 2
                });
                document.getElementById('amount').value = value;
                document.getElementById('amount').setAttribute('data-formatted', formatted);
            }
        });
    });

    // Duplicar imágenes para efecto de ciclo infinito
    const track = document.querySelector('.gallery-track');
    if (track) {
        const items = Array.from(track.children);
        items.forEach(item => {
            const clone = item.cloneNode(true);
            track.appendChild(clone);
        });
    }
});
</script>


<script async src="https://www.googletagmanager.com/gtag/js?id=<%= GA_MEASUREMENT_ID %>"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', '<%= GA_MEASUREMENT_ID %>');
</script>

<script src="https://www.google.com/recaptcha/api.js?hl=<%= locale %>" async defer></script>
</body>
</html>
