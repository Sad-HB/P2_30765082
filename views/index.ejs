<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alquiler de Bicicletas Eléctricas y Scooters Urbanos</title>
   
</head>
<body>
    <header>
        <h1>Alquiler de Bicicletas Eléctricas y Scooters Urbanos</h1>
        <nav>
            <ul>
                <li><a href="#descripcion">Descripción</a></li>
                <li><a href="#beneficios">Beneficios</a></li>
                <li><a href="#galeria">Galería</a></li>
                <li><a href="#contact-form">Contacto</a></li>
                <li><a href="#payment-form">Pago</a></li>
                <li><a href="#contacto">Redes Sociales</a></li>
                <li><a href="#contact-list">Lista de Contactos</a></li>
            </ul>
        </nav>
    </header>

    <section id="descripcion">
        <h2>Descripción del Servicio</h2>
        <p>Ofrecemos un servicio de alquiler de <em>bicicletas eléctricas</em> y <em>scooters urbanos</em> para que disfrutes de la ciudad de manera <strong>sostenible</strong> y <strong>divertida</strong>. Nuestros vehículos son fáciles de usar y están disponibles en varias ubicaciones estratégicas.</p>
    </section>

    <section id="beneficios">
        <h2>Beneficios y Ventajas</h2>
        <ul>
            <li><strong>Movilidad rápida y eficiente.</strong></li>
            <li><strong>Reduce la huella de carbono.</strong></li>
            <li><strong>Fácil acceso y disponibilidad en toda la ciudad.</strong></li>
            <li><strong>Precios competitivos y opciones de suscripción.</strong></li>
        </ul>
    </section>

    <section id="contact-form">
        <h2>Formulario de Contacto</h2>
        <form action="/contact/add" method="POST">
            <label for="email">Correo Electrónico:</label>
            <input type="email" id="email" name="email" required>

            <label for="name">Nombres:</label>
            <input type="text" id="name" name="name" required>

            <label for="comment">Comentario:</label>
            <textarea id="comment" name="comment"></textarea>

            <div class="g-recaptcha" data-sitekey="6LdKAUQrAAAAADDN8ugquX7lkGAa8W4StSiVYB8H"></div>
            <button type="submit" class="glow-on-hover">Enviar</button>
        </form>

     
    <div id="contact-confirmation-message" style="display: none; text-align: center; margin-top: 20px;">
        <p style="font-size: 18px; color: #007BFF;">¡Formulario de contacto enviado correctamente!</p>
    </div>

    <section id="payment-form">
        <h2>Formulario de Pago</h2>
        <form action="/payment/add" method="POST">
            <label for="email">Correo Electrónico:</label>
            <input type="email" id="email" name="email" required>

            <label for="cardholderName">Nombre del Titular de la Tarjeta:</label>
            <input type="text" id="cardholderName" name="cardholderName" required>

            <label for="cardNumber">Número de Tarjeta:</label>
            <input type="text" id="cardNumber" name="cardNumber" pattern="^\d{13,19}$" title="El número de tarjeta debe contener entre 13 y 19 dígitos." required>

            <label for="expiryMonth">Mes de Expiración:</label>
            <input type="number" id="expiryMonth" name="expiryMonth" min="1" max="12" required>

            <label for="expiryYear">Año de Expiración:</label>
            <input type="number" id="expiryYear" name="expiryYear" min="2025" required>

            <label for="cvv">Código de Seguridad (CVV):</label>
            <input type="text" id="cvv" name="cvv" pattern="^\d{3,4}$" title="El CVV debe contener 3 o 4 dígitos." required>

            <label for="amount">Monto a Pagar:</label>
            <input type="number" id="amount" name="amount" min="0.01" step="0.01" title="El monto debe ser un número positivo." required>

            <label for="currency">Moneda:</label>
            <select id="currency" name="currency" required>
                <option value="USD">USD</option>
                <option value="EUR">EUR</option>
                <option value="MXN">MXN</option>
            </select>

            <button type="submit" class="glow-on-hover">Pagar</button>
        </form>
    </section>

    <div id="payment-confirmation-message" style="display: none; text-align: center; margin-top: 20px;">
        <p style="font-size: 18px; color: #007BFF;">¡Su pago ha sido aceptado!</p>
    </div>


    <section id="contacto">
        <h2>Contáctanos</h2>
        <p>Síguenos en nuestras redes sociales:</p>
        <a href="https://www.facebook.com/ehenzo.bretpo" target="_blank">Facebook</a>
        <a href="https://twitter.com" target="_blank">Twitter</a>
        <a href="https://www.instagram.com/sad_h.b?igsh=YzljYTk1ODg3Zg==" target="_blank">Instagram</a>
    </section>

    <section id="contact-list">
        <h2>Contactos Registrados</h2>
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Correo Electrónico</th>
                    <th>Nombres</th>
                    <th>Comentario</th>
                    <th>IP</th>
                    <th>Fecha y Hora</th>
                    <th>País</th>
                </tr>
            </thead>
            <tbody>
                <% var contacts = typeof contacts !== 'undefined' ? contacts : []; %>
                <% if (contacts && contacts.length > 0) { %>
                    <% contacts.forEach(contact => { %>
                        <tr>
                            <td data-label="ID"><%= contact.id %></td>
                            <td data-label="Correo Electrónico"><%= contact.email %></td>
                            <td data-label="Nombres"><%= contact.name %></td>
                            <td data-label="Comentario"><%= contact.comment %></td>
                            <td data-label="IP"><%= contact.ip %></td>
                            <td data-label="Fecha y Hora"><%= contact.timestamp %></td>
                            <td data-label="País"><%= contact.country %></td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="7">No hay contactos registrados.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </section>
    
    <footer>
        <p>&copy; 2025 Alquiler de Bicicletas Eléctricas y Scooters Urbanos. Todos los derechos reservados.</p>
    </footer>
<style>
    
body{
    font-family: Arial, sans-serif;
    line-height: 1.6;
    margin: 0;
    padding: 0;
    background-color: #f4f4f4;
}

header {
    background: #007BFF;
    color: #ffffff;
    padding: 10px 0;
    text-align: center;
}

nav ul {
    list-style: none;
    padding: 0;
}

nav ul li {
    display: inline;
    margin: 0 15px;
}

nav ul li a {
    color: #ffffff;
    text-decoration: none;
}

nav ul li a:hover {
    text-decoration: underline;
}

section {
    padding: 20px;
    margin: 20px;
    background: #ffffff;
    border-radius: 5px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

h1, h2 {
    color: #333;
}

h2 {
    border-bottom: 2px solid #007BFF;
    padding-bottom: 5px;
}

footer {
    text-align: center;
    padding: 10px 0;
    background: #007BFF;
    color: #ffffff;
    position: relative;
    bottom: 0;
    width: 100%;
}

form input, form textarea, form select {
    width: 40%;
    padding: 10px;
    margin: 20px 0; 
    border: 1px solid #ccc;
    border-radius: 5px;
    transition: all 0.3s ease; 
}

form input:focus, form textarea:focus, form select:focus {
    border-color: #007BFF;
    box-shadow: 0 0 10px rgba(0, 123, 255, 0.5);
    outline: none;
    transform: scale(1.02); 
}

form button {
    background-color: #007BFF;
    color: #fff;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.2s ease;
    margin-top: 20px; 
}

form button:hover {
    background-color: #0056b3;
    transform: scale(1.05); 
}

form button:active {
    transform: scale(0.98);
}

form label {
    display: block;
    margin-bottom: 5px; 
    font-weight: bold;
}

form textarea {
    resize: none; 
}

.glow-on-hover {
    width: 220px;
    height: 50px;
    border: none;
    outline: none;
    color: #fff;
    background: #111;
    cursor: pointer;
    position: relative;
    z-index: 0;
    border-radius: 10px;
}

.glow-on-hover:before {
    content: '';
    background: linear-gradient(45deg, #ff0000, #ff7300, #fffb00, #48ff00, #00ffd5, #002bff, #7a00ff, #ff00c8, #ff0000);
    position: absolute;
    top: -2px;
    left: -2px;
    background-size: 400%;
    z-index: -1;
    filter: blur(5px);
    width: calc(100% + 4px);
    height: calc(100% + 4px);
    animation: glowing 20s linear infinite;
    opacity: 0;
    transition: opacity .3s ease-in-out;
    border-radius: 10px;
}

.glow-on-hover:active {
    color: #000;
}

.glow-on-hover:active:after {
    background: transparent;
}

.glow-on-hover:hover:before {
    opacity: 1;
}

.glow-on-hover:after {
    z-index: -1;
    content: '';
    position: absolute;
    width: 100%;
    height: 100%;
    background: #111;
    left: 0;
    top: 0;
    border-radius: 10px;
}

@keyframes glowing {
    0% { background-position: 0 0; }
    50% { background-position: 400% 0; }
    100% { background-position: 0 0; }
}

#contact-preview table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px;
    overflow-x: auto;
    display: block;
}

#contact-preview th, #contact-preview td {
    border: 1px solid #ddd;
    padding: 8px;
    text-align: left;
    white-space: nowrap; 
}

#contact-preview th {
    background-color: #007BFF;
    color: white;
    font-weight: bold;
}

#contact-preview tr:nth-child(even) {
    background-color: #f2f2f2;
}

#contact-preview tr:hover {
    background-color: #ddd;
}

#contact-list table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px;
    background: #fff;
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    table-layout: auto;
}
#contact-list th, #contact-list td {
    border: 1px solid #e0e0e0;
    padding: 12px 10px;
    text-align: left;
    font-size: 16px;
    word-break: break-word;
}
#contact-list th {
    background-color: #007BFF;
    color: #fff;
    font-weight: bold;
    letter-spacing: 0.5px;
}
#contact-list tbody tr:nth-child(even) {
    background-color: #f8faff;
}
#contact-list tbody tr:nth-child(odd) {
    background-color: #f4f4f4;
}
#contact-list tbody tr:hover {
    background-color: #e3f0ff;
    transition: background 0.2s;
}
#contact-list td {
    vertical-align: middle;
}
@media (max-width: 900px) {
    #contact-list table, #contact-list thead, #contact-list tbody, #contact-list th, #contact-list td, #contact-list tr {
        display: block;
    }
    #contact-list thead tr {
        display: none;
    }
    #contact-list td {
        position: relative;
        padding-left: 50%;
        min-height: 40px;
        border: none;
        border-bottom: 1px solid #e0e0e0;
        font-size: 15px;
    }
    #contact-list td:before {
        position: absolute;
        top: 12px;
        left: 10px;
        width: 45%;
        white-space: nowrap;
        font-weight: bold;
        color: #007BFF;
        content: attr(data-label);
    }
    #contact-list table {
        box-shadow: none;
        border-radius: 0;
    }
}
@media (max-width: 600px) {
    #contact-list td {
        font-size: 13px;
        padding: 10px 5px 10px 45%;
    }
}
</style>

<script>
let contactIdCounter = 1; 

const contactForm = document.querySelector('#contact-form form');
contactForm.addEventListener('submit', async function (event) {
    event.preventDefault(); 
    const confirmationMessage = document.getElementById('contact-confirmation-message');
    confirmationMessage.style.display = 'none';

    // Validar reCAPTCHA antes de enviar
    const recaptchaResponse = grecaptcha.getResponse();
    if (!recaptchaResponse) {
        confirmationMessage.innerHTML = '<p style="font-size: 18px; color: red;">Por favor, confirma el reCAPTCHA antes de enviar el formulario.</p>';
        confirmationMessage.style.display = 'block';
        return;
    }

    try {
        // Enviar como application/x-www-form-urlencoded para que el backend reciba los datos correctamente
        const formData = new URLSearchParams();
        formData.append('email', contactForm.email.value);
        formData.append('name', contactForm.name.value);
        formData.append('comment', contactForm.comment.value);
        formData.append('g-recaptcha-response', recaptchaResponse);
        // DEBUG: Mostrar los valores enviados
        for (let [key, value] of formData.entries()) {
            console.log('FORMDATA', key, value);
        }
        const response = await fetch('/contact/add', {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
            body: formData.toString()
        });
        const result = await response.json();
        if (result.success) {
            confirmationMessage.innerHTML = '<p style="font-size: 18px; color: #007BFF;">¡Formulario de contacto enviado correctamente!</p>';
            confirmationMessage.style.display = 'block';
            contactForm.reset();
            grecaptcha.reset(); 
        } else {
            confirmationMessage.innerHTML = '<p style="font-size: 18px; color: red;">' + (result.message || 'Error al enviar el formulario') + '</p>';
            confirmationMessage.style.display = 'block';
        }
    } catch (error) {
        confirmationMessage.innerHTML = '<p style="font-size: 18px; color: red;">Error al enviar el formulario</p>';
        confirmationMessage.style.display = 'block';
    }
});

document.querySelector('#payment-form form').addEventListener('submit', function (event) {
    event.preventDefault(); 
    const confirmationMessage = document.getElementById('payment-confirmation-message');
    confirmationMessage.style.display = 'block'; 
    setTimeout(() => {
        this.submit(); 
    }, 2000); 
});

function showContactPreview(event) {
    event.preventDefault(); 

   
    const id = contactIdCounter++; 
    const email = document.getElementById('email').value;
    const name = document.getElementById('name').value;
    const comment = document.getElementById('comment').value;
    const ip = '127.0.0.1'; 
    const timestamp = new Date().toLocaleString(); 

 
    document.getElementById('preview-id').textContent = id;
    document.getElementById('preview-email').textContent = email;
    document.getElementById('preview-name').textContent = name;
    document.getElementById('preview-comment').textContent = comment;
    document.getElementById('preview-ip').textContent = ip;
    document.getElementById('preview-timestamp').textContent = timestamp;

    document.getElementById('email').value = email;
    document.getElementById('name').value = name;
    document.getElementById('comment').value = comment;

    document.getElementById('contact-preview').style.display = 'block';
}

window.addEventListener('DOMContentLoaded', function () {
    const dateCells = document.querySelectorAll('#contact-list tbody tr td:nth-child(6)');
    dateCells.forEach(cell => {
        const raw = cell.textContent;
        if (!raw || raw === 'Fecha y Hora') return;
        const date = new Date(raw);
        if (!isNaN(date.getTime())) {
            const options = { year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' };
            cell.textContent = date.toLocaleString('es-ES', options);
        }
    });
});
</script>


 
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-T4W09XMGDV"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-T4W09XMGDV');
</script>
<!-- Fin Google Analytics -->

<!-- Google reCAPTCHA v2 -->
<script src="https://www.google.com/recaptcha/api.js" async defer></script>
</body>
</html>
